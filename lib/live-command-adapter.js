'use strict';
const { CommandRegistry } = require('./command-registry');
const { dispatch } = require('./command-router');
const { COMMAND_CATEGORIES, categoryFor } = require('./command-categories');
const { manageParticipants, groupRoleState, assertGroupAdmin } = require('./group-participant-service');
const { setGroupName, getGroupInviteLink } = require('./group-metadata-service');
const { registerPlannedFeatures } = require('./planned-features');
const LEGACY_ALIASES = Object.freeze({ help: 'menu', del: 'delete' });
const PLANNED_OVERRIDES = new Set(['autoread','autoreact','autotyping','autorecording','autorecordtype','autostatus','autostatusview','autoviewstatus','autoreactstatus','autolikestatus','presence','anticall','antistatus','antilink']);
function normalizeCategory(categoryOrName, legacyCategory) { if (legacyCategory === undefined) { const value=String(categoryOrName||'').toUpperCase(); if(value==='GENERAL')return COMMAND_CATEGORIES.CORE; if(Object.values(COMMAND_CATEGORIES).includes(value))return value; return COMMAND_CATEGORIES.UTILITY; } const fallback=String(legacyCategory||'').toUpperCase(); return categoryFor(categoryOrName,Object.values(COMMAND_CATEGORIES).includes(fallback)?fallback:COMMAND_CATEGORIES.UTILITY); }
function groupJid(ctx){return ctx?.chatId||ctx?.rawMessage?.key?.remoteJid||ctx?.rawMessage?.key?.remoteJidAlt||''}
function actorJid(ctx){return ctx?.actor||ctx?.senderId||ctx?.rawMessage?.key?.participant||ctx?.rawMessage?.key?.participantAlt||''}
function groupOnlyMessage(ctx){return ctx?.sock?.sendMessage(ctx.chatId,{text:'ℹ️ This command is for groups only.'},{quoted:ctx.rawMessage})}
async function requireGroupAdmin(ctx){const group=groupJid(ctx);if(!group.endsWith('@g.us'))throw new Error('This command is for groups only.');const state=await groupRoleState(ctx.sock,group,actorJid(ctx));assertGroupAdmin(state);return state}
function registerPhase1Commands(registry){const register=d=>{if(!registry.has(d.name))registry.register(d)};for(const action of ['add','remove','promote','demote'])register({name:action==='remove'?'kick':action,description:`${action==='remove'?'Remove':action[0].toUpperCase()+action.slice(1)} group participant`,category:COMMAND_CATEGORIES.GROUP,groupOnly:true,handler:async ctx=>{const group=groupJid(ctx);if(!group.endsWith('@g.us'))return groupOnlyMessage(ctx);const result=await manageParticipants(ctx.sock,group,actorJid(ctx),ctx.args||[],action);const labels=result.targets.map(t=>`@${String(t.jid||t.id).split('@')[0]}`);return ctx.sock.sendMessage(group,{text:`✅ ${action==='remove'?'Removed':action[0].toUpperCase()+action.slice(1)}: ${labels.join(', ')}`,mentions:result.jids},{quoted:ctx.rawMessage})}});register({name:'gname',description:'Change the group name',category:COMMAND_CATEGORIES.GROUP,groupOnly:true,handler:async ctx=>{const group=groupJid(ctx);if(!group.endsWith('@g.us'))return groupOnlyMessage(ctx);await requireGroupAdmin(ctx);const name=(ctx.args||[]).join(' ').trim();await setGroupName(ctx.sock,group,name);return ctx.sock.sendMessage(group,{text:`✅ Group name changed to: ${name}`},{quoted:ctx.rawMessage})}});register({name:'grouplink',aliases:['gclink','invite'],description:'Get the group invite link',category:COMMAND_CATEGORIES.GROUP,groupOnly:true,handler:async ctx=>{const group=groupJid(ctx);if(!group.endsWith('@g.us'))return groupOnlyMessage(ctx);await requireGroupAdmin(ctx);const link=await getGroupInviteLink(ctx.sock,group);return ctx.sock.sendMessage(group,{text:`🔗 Group link:\n${link}`},{quoted:ctx.rawMessage})}})}
function createRegistryFromLegacyMap(commands){if(!commands||typeof commands.entries!=='function')throw new TypeError('A legacy command Map is required.');const registry=new CommandRegistry(),deferredAliases=[];for(const [rawName,item] of commands.entries()){const name=String(rawName||'').trim().toLowerCase();if(!item||typeof item.handler!=='function')continue;if(PLANNED_OVERRIDES.has(name))continue;const aliasTarget=LEGACY_ALIASES[name];if(aliasTarget&&commands.has(aliasTarget)){deferredAliases.push([name,aliasTarget]);continue}registry.register({name,description:item.description||'',category:normalizeCategory(name,item.category),ownerOnly:Boolean(item.ownerOnly),handler:async ctx=>item.handler(ctx.sock,ctx.rawMessage||ctx.message,ctx.args||[])})}for(const [alias,target] of deferredAliases)if(registry.has(target)&&!registry.has(alias))registry.registerAlias(alias,target);registerPhase1Commands(registry);registerPlannedFeatures(registry);return registry}
function createLegacyDispatchAdapter(commands,options={}){const registry=createRegistryFromLegacyMap(commands);return{registry,dispatch:ctx=>dispatch(ctx,registry,options)}}
module.exports={createRegistryFromLegacyMap,createLegacyDispatchAdapter,normalizeCategory,registerPhase1Commands,LEGACY_ALIASES,PLANNED_OVERRIDES};
